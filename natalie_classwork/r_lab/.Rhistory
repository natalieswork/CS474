data <- read_csv('faithful.csv',show_col_types = FALSE)
library(tidyverse)
install.packages("tidyverse")
library(tidyverse)
faithful <- read_csv('faithful.csv',show_col_types = FALSE)
head(faithful, 5)
duration = faithful$eruptions      # the eruption durations
waiting = faithful$waiting         # the waiting interval
plot(duration, waiting,            # plot the variables
+   xlab="Eruption duration (min)",        # x−axis label
duration = faithful$eruptions      # the eruption durations
waiting = faithful$waiting         # the waiting interval
plot(duration, waiting,            # plot the variables
+   xlab("Eruption duration (min)"),        # x−axis label
+   ylab="Time waited (min)")              # y−axis label
duration = faithful$eruptions      # the eruption durations
waiting = faithful$waiting         # the waiting interval
plot(duration, waiting,            # plot the variables
+   xlab("Eruption duration (min)"),        # x−axis label
+   ylab="Time waited (min)")              # y−axis label
duration = faithful$eruptions      # the eruption durations
waiting = faithful$waiting         # the waiting interval
plot(duration, waiting,            # plot the variables
+   xlab="Eruption duration (min)",        # x−axis label
duration = faithful$eruptions      # the eruption durations
waiting = faithful$waiting         # the waiting interval
plot(duration, waiting,            # plot the variables
+ xlab="Eruption duration (min)",        # x−axis label
duration = faithful$eruptions      # the eruption durations
waiting = faithful$waiting         # the waiting interval
plot(duration, waiting,            # plot the variables
+ xlab="Eruption duration (min)",        # x−axis label
duration = faithful$eruptions      # the eruption durations
waiting = faithful$waiting         # the waiting interval
plot(duration, waiting,            # plot the variables
xlab="Eruption duration (min)",        # x−axis label
ylab="Time waited (min)",              # y−axis label
abline(lm(waiting ~ duration))
duration <- faithful$eruptions      # the eruption durations
waiting <- faithful$waiting         # the waiting interval
plot(duration, waiting,            # plot the variables
xlab="Eruption duration (min)",        # x−axis label
ylab="Time waited (min)")              # y−axis label
abline(lm(waiting ~ duration))      # add a regression line
duration <- faithful$eruptions      # the eruption durations
waiting <- faithful$waiting         # the waiting interval
plot(duration, waiting,            # plot the variables
xlab="Previous Eruption Duration (min)",        # x−axis label
ylab="Time waited (min)")              # y−axis label
abline(lm(waiting ~ duration))      # add a regression line
duration <- faithful$eruptions      # the eruption durations
waiting <- faithful$waiting         # the waiting interval
plot(duration, waiting,            # plot the variables
xlab="Eruption Duration (min)",        # x−axis label
ylab="Time waited (min)")              # y−axis label
abline(lm(waiting ~ duration))      # add a regression line
duration <- faithful$eruptions      # the eruption durations
waiting <- faithful$waiting         # the waiting interval
plot(duration, waiting,            # plot the variables
xlab="Eruption Duration (min)",        # x−axis label
ylab="Time Waited (min)")              # y−axis label
abline(lm(waiting ~ duration))      # add a regression line
duration = faithful$eruptions
stem(duration)
duration = faithful$eruptions
stem(duration, scale = 1, width = 20, col.stems = "steelblue", col.leaf = "darkorange")
duration = faithful$eruptions
stem(duration, scale = 1, width = 20)
duration = faithful$eruptions
stem(duration)
waiting = faithful$waiting
stem(waiting)
duration = faithful$eruptions
breaks = seq(1.5, 5.5, by=0.5)
duration.cut = cut(duration, breaks, right=FALSE)
duration.freq = table(duration.cut)
duration.cumfreq = cumsum(duration.freq)
duration.cumrelfreq = duration.cumfreq / nrow(faithful)
cumrelfreq0 = c(0, duration.cumrelfreq)
plot(breaks, cumrelfreq0,
main="Old Faithful Eruptions",  # main title
xlab="Duration minutes",
ylab="Cumulative eruption proportion")
lines(breaks, cumrelfreq0)        # join the points
waiting = faithful$waiting
breaks = seq(1.5, 5.5, by=0.5)
waiting.cut = cut(waiting, breaks, right=FALSE)
waiting.freq = table(waiting.cut)
waiting.cumfreq = cumsum(waiting.freq)
waiting.cumrelfreq = waiting.cumfreq / nrow(faithful)
cumrelfreq0 = c(0, duration.cumrelfreq)
plot(breaks, cumrelfreq0,
main="Old Faithful Waiting Times",  # main title
xlab="Waiting Times (mins)",
ylab="Cumulative waiting time proportion")
lines(breaks, cumrelfreq0)        # join the points
waiting = faithful$waiting
stem(waiting)
waiting = faithful$waiting
breaks = seq(40, 100, by=10)
waiting.cut = cut(waiting, breaks, right=FALSE)
waiting.freq = table(waiting.cut)
waiting.cumfreq = cumsum(waiting.freq)
waiting.cumrelfreq = waiting.cumfreq / nrow(faithful)
cumrelfreq0 = c(0, duration.cumrelfreq)
plot(breaks, cumrelfreq0,
main="Old Faithful Waiting Times",  # main title
xlab="Waiting Times (mins)",
ylab="Cumulative waiting time proportion")
waiting = faithful$waiting
breaks = seq(40, 100, by=5)
waiting.cut = cut(waiting, breaks, right=FALSE)
waiting.freq = table(waiting.cut)
waiting.cumfreq = cumsum(waiting.freq)
waiting.cumrelfreq = waiting.cumfreq / nrow(faithful)
cumrelfreq0 = c(0, duration.cumrelfreq)
plot(breaks, cumrelfreq0,
main="Old Faithful Waiting Times",  # main title
xlab="Waiting Times (mins)",
ylab="Cumulative waiting time proportion")
waiting = faithful$waiting
breaks = seq(40, 100, by=5)
waiting.cut = cut(waiting, breaks, right=FALSE)
waiting.freq = table(waiting.cut)
waiting.cumfreq = cumsum(waiting.freq)
waiting.cumrelfreq = waiting.cumfreq / nrow(faithful)
cumrelfreq0 = c(0, waiting.cumrelfreq)
plot(breaks, cumrelfreq0,
main="Old Faithful Waiting Times",  # main title
xlab="Waiting Times (mins)",
ylab="Cumulative waiting time proportion")
lines(breaks, cumrelfreq0)        # join the points
cumrelfreq0 = c(0, waiting.cumrelfreq)
plot(breaks, cumrelfreq0,
main="Old Faithful Waiting Times",  # main title
xlab="Waiting Times (mins)",
ylab="Cumulative waiting time proportion")
lines(breaks, cumrelfreq0)        # join the points
duration <- faithful$eruptions      # the eruption durations
waiting <- faithful$waiting         # the waiting interval
plot(duration, waiting,            # plot the variables
xlab="Eruption Duration (min)",        # x−axis label
ylab="Time Waited (min)")              # y−axis label
abline(lm(waiting ~ duration))      # add a regression line
waiting = faithful$waiting
breaks = seq(40, 100, by=5)
waiting.cut = cut(waiting, breaks, right=FALSE)
waiting.freq = table(waiting.cut)
waiting.cumfreq = cumsum(waiting.freq)
waiting.cumrelfreq = waiting.cumfreq / nrow(faithful)
library(tidyverse)
faithful <- read_csv('faithful.csv',show_col_types = FALSE)
head(faithful, 5)
faithful <- read_csv('faithful.csv',show_col_types = FALSE)
head(faithful, 5)
waiting.min
faithful <- read_csv('faithful.csv',show_col_types = FALSE)
head(faithful, 5)
waiting=faithful$waiting
duration=faithful$eruptions
min(waiting)
faithful <- read_csv('faithful.csv',show_col_types = FALSE)
head(faithful, 5)
waiting=faithful$waiting
duration=faithful$eruptions
minw=min(waiting)
minw
faithful <- read_csv('faithful.csv',show_col_types = FALSE)
head(faithful, 5)
waiting=faithful$waiting
duration=faithful$eruptions
minw=min(waiting)
maxw=max(waiting)
faithful <- read_csv('faithful.csv',show_col_types = FALSE)
head(faithful, 5)
waiting=faithful$waiting
duration=faithful$eruptions
minw=min(waiting)
maxw=max(waiting)
mind=min(duration)
maxd=max(duration)
duration = faithful$eruptions
breaks = seq(1.5, 5.5, by=0.5)
duration.cut = cut(duration, breaks, right=FALSE)
duration.freq = table(duration.cut)
duration.relfreq = duration.freq / nrow(faithful)
duration.relfreq
old = options(digits=1)
cbind(duration.freq, duration.relfreq)
waiting = faithful$waiting
breaks = seq(40, 100, by=5)
waiting.cut = cut(waiting, breaks, right=FALSE)
waiting.freq = table(waiting.cut)
waiting.relfreq = waiting.freq / nrow(faithful)
waiting.relfreq
old = options(digits=1)
cbind(waiting.freq, duration.relfreq)
waiting = faithful$waiting
breaks = seq(40, 100, by=5)
waiting.cut = cut(waiting, breaks, right=FALSE)
waiting.freq = table(waiting.cut)
waiting.relfreq = waiting.freq / nrow(faithful)
waiting.relfreq
old = options(digits=1)
cbind(waiting.freq, waiting.relfreq)
waiting = faithful$waiting
breaks = seq(40, 100, by=5)
waiting.cut = cut(waiting, breaks, right=FALSE)
waiting.freq = table(waiting.cut)
waiting.cumfreq = cumsum(waiting.freq)
waiting.cumrelfreq = waiting.cumfreq / nrow(faithful)
cbind(waiting.cumfreq)
waiting = faithful$waiting
breaks = seq(40, 100, by=10)
waiting.cut = cut(waiting, breaks, right=FALSE)
waiting.freq = table(waiting.cut)
waiting.cumfreq = cumsum(waiting.freq)
waiting.cumrelfreq = waiting.cumfreq / nrow(faithful)
cbind(waiting.cumfreq)
waiting = faithful$waiting
breaks = seq(40, 100, by=5)
waiting.cut = cut(waiting, breaks, right=FALSE)
waiting.freq = table(waiting.cut)
waiting.cumfreq = cumsum(waiting.freq)
waiting.cumrelfreq = waiting.cumfreq / nrow(faithful)
cbind(waiting.cumfreq)
duration = faithful$eruptions
hist(duration,    # apply the hist function
right=FALSE)    # intervals closed on the left
colors = c("red", "yellow", "green", "violet", "orange",
"blue", "pink", "cyan")
hist(duration,    # apply the hist function
right=FALSE,    # intervals closed on the left
col=colors,     # set the color palette
main="Old Faithful Eruptions", # the main title
xlab="Duration minutes")       # x-axis label
waiting = faithful$waiting
hist(waiting,    # apply the hist function
right=FALSE)    # intervals closed on the left
colors = c("red", "yellow", "green", "violet", "orange",
"blue", "pink", "cyan")
hist(waiting    # apply the hist function
right=FALSE,    # intervals closed on the left
waiting = faithful$waiting
colors = c("red", "yellow", "green", "violet", "orange",
"blue", "pink", "cyan")
hist(waiting    # apply the hist function
right=FALSE,    # intervals closed on the left
waiting = faithful$waiting
colors = c("red", "yellow", "green", "violet", "orange", "blue", "pink", "cyan")
hist(waiting,    # apply the hist function
right=FALSE,    # intervals closed on the left
col=colors,     # set the color palette
main="Old Faithful Waiting Times", # the main title
xlab="Waiting Times (min)")       # x-axis label
waiting = faithful$waiting
hist(waiting,    # apply the hist function
right=FALSE,    # intervals closed on the left
main="Old Faithful Waiting Times", # the main title
xlab="Waiting Times (min)")       # x-axis label
waiting = faithful$waiting
breaks = seq(40, 100, by=5)
waiting.cut = cut(waiting, breaks, right=FALSE)
waiting.freq = table(waiting.cut)
cbind(waiting.freq)
waiting = faithful$waiting
breaks = seq(40, 100, by=5)
waiting.cut = cut(waiting, breaks, right=FALSE)
waiting.freq = table(waiting.cut)
cbind(waiting.freq)
names(waiting.freq)[which.max(waiting.freq)]
waiting = faithful$waiting
breaks = seq(40, 100, by=10)
waiting.cut = cut(waiting, breaks, right=FALSE)
waiting.freq = table(waiting.cut)
cbind(waiting.freq)
names(waiting.freq)[which.max(waiting.freq)]
waiting = faithful$waiting
hist(waiting,    # apply the hist function
right=FALSE,    # intervals closed on the left
main="Old Faithful Waiting Times", # the main title
xlab="Waiting Times (min)")       # x-axis label
waiting = faithful$waiting
breaks = seq(40, 100, by=10)
waiting.cut = cut(waiting, breaks, right=FALSE)
waiting.freq = table(waiting.cut)
waiting.relfreq = waiting.freq / nrow(faithful)
waiting.relfreq
old = options(digits=1)
cbind(waiting.freq, waiting.relfreq)
waiting = faithful$waiting
breaks = seq(40, 100, by=10)
waiting.cut = cut(waiting, breaks, right=FALSE)
waiting.freq = table(waiting.cut)
waiting.cumfreq = cumsum(waiting.freq)
waiting.cumrelfreq = waiting.cumfreq / nrow(faithful)
cbind(waiting.cumfreq)
cumrelfreq0 = c(0, waiting.cumrelfreq)
plot(breaks, cumrelfreq0,
main="Old Faithful Waiting Times",  # main title
xlab="Waiting Times (mins)",
ylab="Cumulative waiting time proportion")
lines(breaks, cumrelfreq0)        # join the points
waiting = faithful$waiting
stem(waiting)
duration <- faithful$eruptions      # the eruption durations
waiting <- faithful$waiting         # the waiting interval
plot(duration, waiting,            # plot the variables
xlab="Eruption Duration (min)",        # x−axis label
ylab="Time Waited (min)")              # y−axis label
abline(lm(waiting ~ duration))      # add a regression line
duration <- faithful$eruptions      # the eruption durations
waiting <- faithful$waiting         # the waiting interval
plot(duration, waiting,            # plot the variables
main="Eruption Duration vs. Waiting Times",  # main title
xlab="Eruption Duration (min)",        # x−axis label
ylab="Time Waited (min)")              # y−axis label
abline(lm(waiting ~ duration))      # add a regression line
duration <- faithful$eruptions      # the eruption durations
waiting <- faithful$waiting         # the waiting interval
plot(duration, waiting,            # plot the variables
main="Eruption Duration vs. Waiting Times",  # main title
xlab="Eruption Duration (min)",        # x−axis label
ylab="Time Waiting (min)")              # y−axis label
abline(lm(waiting ~ duration))      # add a regression line
cumrelfreq0 = c(0, waiting.cumrelfreq)
plot(breaks, cumrelfreq0,
main="Old Faithful Waiting Times",  # main title
xlab="Waiting Times (min)",
ylab="Cumulative Waiting Time Proportion")
lines(breaks, cumrelfreq0)        # join the points
waiting = faithful$waiting
breaks = seq(40, 100, by=10)
waiting.cut = cut(waiting, breaks, right=FALSE)
waiting.freq = table(waiting.cut)
waiting.cumfreq = cumsum(waiting.freq)
waiting.cumrelfreq = waiting.cumfreq / nrow(faithful)
cbind(waiting.cumfreq)
install_tinytex()\
install_tinytex()
waiting = faithful$waiting
breaks = seq(40, 100, by=10)
waiting.cut = cut(waiting, breaks, right=FALSE)
waiting.freq = table(waiting.cut)
cbind(waiting.freq)
names(waiting.freq)[which.max(waiting.freq)]
waiting = faithful$waiting
breaks = seq(40, 100, by=10)
waiting.cut = cut(waiting, breaks, right=FALSE)
waiting.freq = table(waiting.cut)
cbind(waiting.freq)
names(waiting.freq)[which.max(waiting.freq)]
waiting = faithful$waiting
hist(waiting,    # apply the hist function
right=FALSE,    # intervals closed on the left
main="Old Faithful Waiting Times", # the main title
xlab="Waiting Times (min)")       # x-axis label
waiting = faithful$waiting
hist(waiting,    # apply the hist function
right=FALSE,    # intervals closed on the left
main="Old Faithful Waiting Times", # the main title
xlab="Waiting Times (min)")       # x-axis label
waiting = faithful$waiting
hist(waiting,    # apply the hist function
right=FALSE,    # intervals closed on the left
main="Old Faithful Waiting Times", # the main title
xlab="Waiting Times (min)")       # x-axis label
waiting = faithful$waiting
breaks = seq(40, 100, by=10)
waiting.cut = cut(waiting, breaks, right=FALSE)
waiting.freq = table(waiting.cut)
waiting.relfreq = waiting.freq / nrow(faithful)
waiting.relfreq
old = options(digits=1)
cbind(waiting.freq, waiting.relfreq)
waiting = faithful$waiting
breaks = seq(40, 100, by=10)
waiting.cut = cut(waiting, breaks, right=FALSE)
waiting.freq = table(waiting.cut)
waiting.relfreq = waiting.freq / nrow(faithful)
old = options(digits=1)
cbind(waiting.relfreq)
waiting = faithful$waiting
breaks = seq(40, 100, by=10)
waiting.cut = cut(waiting, breaks, right=FALSE)
waiting.freq = table(waiting.cut)
waiting.relfreq = waiting.freq / nrow(faithful)
old = options(digits=1)
cbind(waiting.relfreq)
cumrelfreq0 = c(0, waiting.cumrelfreq)
plot(breaks, cumrelfreq0,
main="Old Faithful Waiting Times",  # main title
xlab="Waiting Times (min)",
ylab="Cumulative Waiting Time Proportion")
lines(breaks, cumrelfreq0)        # join the points
waiting = faithful$waiting
breaks = seq(40, 100, by=10)
waiting.cut = cut(waiting, breaks, right=FALSE)
waiting.freq = table(waiting.cut)
waiting.cumfreq = cumsum(waiting.freq)
waiting.cumrelfreq = waiting.cumfreq / nrow(faithful)
cbind(waiting.cumfreq)
waiting = faithful$waiting
stem(waiting)
cumrelfreq0 = c(0, waiting.cumrelfreq)
plot(breaks, cumrelfreq0,
main="Cumulative Frequency Graph of Eruption Waiting Periods",  # main title
xlab="Waiting Times (min)",
ylab="Cumulative Waiting Time Proportion")
lines(breaks, cumrelfreq0)        # join the points
duration <- faithful$eruptions      # the eruption durations
waiting <- faithful$waiting         # the waiting interval
plot(duration, waiting,            # plot the variables
main="Eruption Duration vs. Waiting Times",  # main title
xlab="Eruption Duration (min)",        # x−axis label
ylab="Time Waiting (min)")              # y−axis label
abline(lm(waiting ~ duration))      # add a regression line
library(tidyverse)
install_tinytex()
